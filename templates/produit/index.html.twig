{% extends 'base.html.twig' %}

{% block title %}Produit index{% endblock %}

{% block body %}
    <h1>Produit index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Description</th>
                <th>Catégorie</th>
                <th>Image</th>
                
            </tr>
        </thead>
        <tbody>
        {% for produit in produits %}
            <tr>
                <td>{{ produit.id }}</td>
                <td>{{ produit.nom }}</td>
                <td>{{ produit.description }}</td>
                <td>{{ produit.relationCate.nom }}</td>
            <td>
                {% if produit.imageUrl %}
                    <img src="{{ produit.imageUrl }}" alt="photo" width="100" /><br />
                {% else %}
                    -
                {% endif %}
            </td>
            <td>
                {# <a href="{{ path('app_produit_show', {'id': produit.id}) }}">show</a> #}
                {# <a href="{{ path('app_produit_edit', {'id': produit.id}) }}">edit</a> #}
            </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_produit_new') }}">Create new</a>
{% endblock %}
